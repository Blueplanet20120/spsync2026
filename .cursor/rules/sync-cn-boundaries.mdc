---
description: sunnypilot_cn 工具仓与 Gitee 镜像分工；CI 仅改 sync_to_gitee.py
alwaysApply: true
---

# sunnypilot_cn 同步（必读）

## 仓库分工

- **GitHub `Blueplanet20120/sunnypilot_cn`**：放同步**工具**（`tools/sync_to_gitee.py`、`tools/sync_to_gitee_local.py`、`.github/workflows/`、`README`、本地入口 `sp_sync.py` / `sp-sync` 等）。Actions 在这里跑。
- **Gitee `sunnypilot_cn`**：放**打完国内化补丁后的完整 sunnypilot 代码**，供国内拉取更新；由脚本从上游拉取并补丁后**推送**过去。

## `sync_to_gitee.py` 交互菜单（与云端一致）

- **`tools/sync_to_gitee.py`** 内置菜单**仅** **1～3 / 0**：pull、push、一键 all（与 `--action all` 同源补丁逻辑）。
- **mapd / installer**：不在此菜单；云端 Actions **不使用**这两项。请在本机用 **`tools/sync_to_gitee_local.py`**（菜单 4/5）或 CLI：`--sync-mapd-release`、`--build-installer`（脚本/自动化用）。

## 改动边界（避免搞混）

| 场景 | 改哪个脚本 |
|------|------------|
| GitHub Actions / 云端 CI、定时同步、校验、邮件通知 | **仅** [`tools/sync_to_gitee.py`](tools/sync_to_gitee.py) 与工作流 YAML |
| 本机交互菜单（含 4/5）、Windows 路径、自动 clone、comma SSH | [`tools/sync_to_gitee_local.py`](tools/sync_to_gitee_local.py)（及 `sp_sync.py`、`sp-sync`、`setup_local_venv.*`） |

**禁止**：为 CI/云端需求去改 `tools/sync_to_gitee_local.py`。本地封装脚本**不参与**云端逻辑。

## 实施前自检

- 若任务写「Actions」「workflow」「定时」「Gitee 推送」「verify_patches」「邮件通知」→ 只动 `sync_to_gitee.py` / `.github/workflows/sync-to-gitee.yml`。
- 若任务明确「本地菜单」「comma SSH」「选项 4/5」→ 再考虑 `sync_to_gitee_local.py`。
- 如果只是推送或者同步，都是指的推送到 Blueplanet20120/sunnypilot_cn。
- 连接不上没有密钥这些可要考虑看看本项目 `.env`（勿提交仓库）。
